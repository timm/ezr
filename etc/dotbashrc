export BASH_SILENCE_DEPRECATION_WARNING=1

# Function to get the current git branch
function parse_git_branch {
  git branch 2>/dev/null | grep '*' | sed 's/* //'
}

# Custom PS1 prompt
PS1='$(parse_git_branch)::$(basename $(dirname "$PWD"))/\W[\#]> '


